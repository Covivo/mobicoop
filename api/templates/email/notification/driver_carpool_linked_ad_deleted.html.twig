{% extends '/email/notification/linked_ad_deleted.html.twig' %}
{% block body %}
    {# <p>{{ "yourCarpoolBetween"|trans }}{{ (ask.matching.waypoints|first).address.county }}{{ "and"|trans }}{{ (ask.matching.waypoints|last).address.county }}{{ "withThePassenger"|trans }}{{ deleterGivenName ~ " " ~ deleterFamilyName }} {{ "isCanceledBecause"|trans }}{{ deleterGivenName ~ " " ~ deleterFamilyName}} {{ "hasDeletedTheAssociateAd"|trans }}</p>
    <p>{{ "clickOnLinkToSearchAgain"|trans }}{{ (ask.matching.waypoints|first).address.county }}{{ "and"|trans }}{{ (ask.matching.waypoints|last).address.county }}. </p>
    <p>{{ "noPenaltyApplied"|trans }}</p> #}
     <p>
        Votre covoiturage entre {{ (ask.matching.waypoints|first).address.county }}
        et {{ (ask.matching.waypoints|last).address.county }}
        avec le passager {{ deleterGivenName ~ " " ~ deleterFamilyName }} a été annulé, car {{ deleterFamilyName ~ " " ~ deleterGivenName }} a supprimé son annonce associée.

        Cliquez sur ce lien pour relancer votre recherche {{ (ask.matching.waypoints|first).address.county }}
        et {{ (ask.matching.waypoints|last).address.county }} . 

        Il n’y a pas de pénalité pour l’annulation de ce covoiturage. Cependant afin de garantir la fiabilité des
        annonces et protéger les covoitureurs d'annulations trop répétées,
        nous nous réservons le droit de suspendre les possibilités de publier des annonces si un covoitureur annule trop
        souvent.
    </p>
{% endblock %}