{% set ask = context.ask %}
{% set recipient = context.user %}
{% if recipient.id == ask.user.id %}
    {% set deleter = ask.userRelated %}
{% else %}
    {% set deleter = ask.user %}
{% endif %}
Bonjour {{recipient.givenName|capitalize}} {{recipient.shortFamilyName|capitalize}}
{{deleter.givenName|capitalize}} {{deleter.shortFamilyName|capitalize}} a annulé son trajet et ne pourra pas covoiturer avec vous de {{ context.origin.address.displayLabel[0] }} à {{ context.destination.address.displayLabel[0] }}. Trouvez de nouveaux covoiturages ou déposez une annonce sur {{api_env('PUSH_PLATFORM_NAME')}}.
A bientôt sur {{api_env('PUSH_PLATFORM_NAME')}}
