{% extends 'communicationAlt/fr/email/notification/base.html.twig' %}
{% block body %}
<table style="width:100%;font-family:{{api_env('EMAILS_FONT')}}, sans-serif;">
    <tr>
        <td>
            <table style="width:100%;">
                <tr>
                    <td style="width:70%">
                        <p style="font-size:13px;">Bonjour<b style="color:{{api_env('EMAILS_COLOR_PRIMARY')}}"> {{context.user.givenName|capitalize}} {{context.user.shortFamilyName|capitalize}}</b></p>
                        <p style="font-size:13px;">{{context.senderAlias|capitalize}} de la plateforme {{context.senderOperator}} a mis à jour une demande pour le covoiturage ci-dessous.</p>
                        <p style="font-size:13px;">Trajet occasionnel:</p>
                        <ul>
                         {% if context.booking.distance %}
                            <li style="font-size:13px;">Distance covoiturée : {{context.booking.distance}}km</li>
                         {% endif %}
                            <li style="font-size:13px;">Date: le {{context.booking.passengerPickupDate|date("d-m-Y à H:i","Europe/Paris")}}</li>
                            <li style="font-size:13px;">Prise en charge à {{context.booking.passengerPickupAddress}}.</li>
                            <li style="font-size:13px;">Dépose à {{context.booking.passengerDropAddress}}.</li>
                         {% if context.booking.price %}
                            <li style="font-size:13px;">Prix indicatif: {{context.booking.price.amount}} {{context.booking.price.currency}}.</li>
                        {% endif %}
                        </ul>
                        <p style="font-size:13px;">Vous pouvez répondre à ce message, donner suite ou refuser le covoiturage, via la messagerie de {{api_env('EMAILS_PLATFORM_NAME')}} en cliquant <a href="{{api_env('EMAILS_PLATFORM_URL')}}/utilisateur/messages">ici</a></p>
                   </td>
                </tr>
            </table>
        </td>
    </tr>
    </hr>
</table>
{% endblock %}
