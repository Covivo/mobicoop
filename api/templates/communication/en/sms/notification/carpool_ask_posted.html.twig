{% set ad = context.ad %}
{% set recipient = context.user %}
{% set sender = context.sender %}
{% set result = context.result %}
{% set outwardOrigin = context.outwardOrigin %}
{% set outwardDestination = context.outwardDestination %}
{% set returnOrigin = context.returnOrigin %}
{% set returnDestination = context.returnDestination %}

Bonjour {{context.user.givenName|capitalize}} {{context.user.shortFamilyName|capitalize}}
{% if ad.results[0].frequency == 1 %}
{{sender.givenName|capitalize}} {{sender.shortFamilyName|capitalize}} vous propose un covoiturage de {{ outwardOrigin.address.displayLabel[0] }} à {{ outwardDestination.address.displayLabel[0] }}. Acceptez ou refusez la demande dans 'Mes Messages' sur {{api_env('SMS_PLATFORM_NAME')|raw}}.
{% else %}
{{sender.givenName|capitalize}} {{sender.shortFamilyName|capitalize}} vous propose un covoiturage régulier de {{ outwardOrigin.address.displayLabel[0] }} à {{ outwardDestination.address.displayLabel[0] }}. Acceptez ou refusez la demande dans 'Mes Messages' sur {{api_env('SMS_PLATFORM_NAME')|raw}}.
{% endif %}
À bientôt sur {{api_env('SMS_PLATFORM_NAME')|raw}}