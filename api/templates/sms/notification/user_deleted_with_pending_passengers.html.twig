{% set ask = context.ask %}
{% set recipient = context.user %}
{% if recipient.id == ask.user.id %}
    {% set deleter = ask.userRelated %}
{% else %}
    {% set deleter = ask.user %}
{% endif %}
Bonjour {{recipient.givenName|capitalize}} {{recipient.shortFamilyName|capitalize}}
{{deleter.givenName|capitalize}} {{deleter.shortFamilyName|capitalize}} a supprimé son compte et ne pourra pas covoiturer avec vous de {{ context.origin.address.displayLabel[0] }} à {{ context.destination.address.displayLabel[0] }}. Trouvez de nouveaux covoiturages ou déposez une annonce sur  {{api_env('SMS_PLATFORM_NAME')|raw}}.
A bientôt sur {{api_env('SMS_PLATFORM_NAME')|raw}}
