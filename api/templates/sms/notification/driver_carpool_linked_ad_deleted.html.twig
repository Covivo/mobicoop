{% set ask = context.ask %}
{% set recipient = context.user %}
{% if recipient.id == ask.user.id %}
    {% set deleter = ask.userRelated %}
{% else %}
    {% set deleter = ask.user %}
{% endif %}
{{"hello"|trans}} {{recipient.givenName|capitalize}} {{recipient.shortFamilyName|capitalize}}
{{recipient.givenName|capitalize}} {{recipient.shortFamilyName|capitalize}} {{"carpoolDeleted"|trans}} {{ context.origin.address.displayLabel[0] }} {{"at"|trans}} {{ context.destination.address.displayLabel[0] }}. {{"findNewCarpool"|trans}} {{api_env('SMS_PLATFORM_NAME')}}.
{{"signature"|trans}} {{api_env('SMS_PLATFORM_NAME')}}