{% set sender = context.object.sender %}
{% set recipient = context.user %}
{% set outwardOrigin = context.origin %}
{% set outwardDestination = context.destination %}

Bonjour {{recipient.givenName|capitalize}} {{recipient.shortFamilyName|capitalize}}

Des détails de votre covoiturage de {{outwardOrigin.address.displayLabel[0]}} à {{outwardDestination.address.displayLabel[0]}} ont été modifiés par {{sender.givenName|capitalize}} {{sender.shortFamilyName|capitalize}} sur {{api_env('SMS_PLATFORM_NAME')|raw}}.

A bientôt sur {{api_env('SMS_PLATFORM_NAME')|raw}}
