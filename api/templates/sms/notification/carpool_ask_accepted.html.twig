{% set ad = context.ad %}
{% set recipient = context.user %}
{% set sender = context.sender %}
{% set result = context.result %}
{% set outwardOrigin = context.outwardOrigin %}
{% set outwardDestination = context.outwardDestination %}
{% set returnOrigin = context.returnOrigin %}
{% set returnDestination = context.returnDestination %}

{{"hello"|trans}} {{context.user.givenName|capitalize}} {{context.user.shortFamilyName|capitalize}}
{% if ad.results[0].frequency == 1 %}
{{"carpoolAskAccepted"|trans}} {{sender.givenName|capitalize}} {{sender.shortFamilyName|capitalize}} {{"the"|trans}} {{ ad.results[0].date|date("d/m/Y") }} {{"at"|trans}} {{outwardOrigin.time|date('H:i')}} {{"from"|trans}} {{ outwardOrigin.address.displayLabel[0] }} {{"at"|trans}} {{ outwardDestination.address.displayLabel[0] }}. {{"seeTheAd"|trans}} {{api_env('SMS_PLATFORM_NAME')}}.
{% else %}
{{"carpoolAskAcceptedRegular"|trans}} {{sender.givenName|capitalize}} {{sender.shortFamilyName|capitalize}} {{"between"|trans}}  {{ outwardOrigin.address.displayLabel[0] }} {{"and"|trans}} {{ outwardDestination.address.displayLabel[0] }}. {{"seeTheAd"|trans}} {{api_env('SMS_PLATFORM_NAME')}}.
{% endif %}
{{"signature"|trans}} {{api_env('SMS_PLATFORM_NAME')}}
